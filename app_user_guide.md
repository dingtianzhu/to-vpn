# ToVpn 桌面客户端使用指南 & 架构优化解析

欢迎使用 **ToVpn**，这是一款基于 **Tauri (Rust)** 与 **Vue 3 + TS** 构建的高性能、现代化的安全 VPN 客户端。本文档将为您提供该桌面客户端的完整使用指南，并深度剖析系统授权弹窗、网络路由错乱及延迟异常等关键技术问题的根因与解决方案。

---

## 📖 桌面应用使用指南

### 1. 首次使用准备 (安装系统助手)
为了能够接管系统级别的网络流量并开启 TUN 模式，客户端需要安装一个轻量级的系统特权助手：
1. 打开应用，进入 **偏好设置 (Settings)** -> **通用 (General)**。
2. 找到 **系统助手 (System Extension)** 配置项。
3. 如果显示“未激活/未安装”，点击 **“安装助手” (Install Helper)**，根据系统提示输入一次 macOS 管理员密码。
4. 安装成功后，状态会变为 **“运行中” (Active)**。

> [!NOTE]
> 系统助手安装仅需在首次使用或助手版本更新时执行一次，后续启动无需重复授权。

### 2. 服务器选择与实时测速
1. 导航至 **服务器列表 (Servers)** 页面。
2. 页面会展示所有可用的服务器节点，以及各节点的地理位置（国旗及城市名称）。
3. **测速机制**：
   - 刚进入页面或手动点击刷新时，服务器右侧会显示蓝色闪烁点和 `- ms`，代表测速测试正在进行。
   - 测速完成后，会实时显示具体的延迟时间（如 `45ms`），并通过颜色区分质量：
     - <span style="color:#10b981">●</span> **绿色** (< 100ms)：极速连接，适合游戏、视频。
     - <span style="color:#f59e0b">●</span> **黄色** (100ms - 200ms)：普通连接，适合网页浏览。
     - <span style="color:#ef4444">●</span> **红色** (> 200ms)：高延迟连接。
   - 如果某个节点服务器处于下线状态或维护中，它的按钮将自动置灰禁用并显示 **“无法连接”** 或 **“维护中”**，防止误点。

### 3. 选择连接模式
在 **“设置”** 中，您可以根据需求选择不同的 **连接模式 (Connection Mode)**：
- **SOCKS 模式**：仅在本地开放 `1080` (SOCKS5) 和 `1081` (HTTP) 代理端口。流量不会强制接管，需要浏览器（配合 SwitchyOmega）或支持代理的应用手动配置该端口。
- **TUN 模式 (推荐)**：创建虚拟网卡 `utunX` 并全面接管整机流量。所有应用无需单独配置代理，即可自动享受加速。

### 4. 路由分流模式
分流模式决定了哪些流量走加速节点，哪些直连：
- **规则模式 (Rule - 推荐)**：**绕过中国大陆**。国内网站（如百度、淘宝）及 `.cn` 域名流量直接连接（不消耗加速流量，访问速度最快）；境外网站流量自动走加速节点。
- **全局模式 (Global)**：整机所有外网访问流量全部通过加速节点。
- **直连模式 (Direct)**：所有流量直连，不进行加速。

### 5. 高级网络功能
- **DNS 泄漏防护**：强制所有非本地域名解析走远程加密 DNS 服务，彻底杜绝运营商劫持与 DNS 请求泄漏。
- **WebRTC 阻断**：阻断浏览器的 STUN/TURN 端口与 WebRTC 交互，防止通过网页协议泄漏您真实的公网物理 IP。
- **自定义分流域名**：
  - **直连域名 (Bypass)**：添加强制直连的域名（如公司内网域名）。
  - **代理域名 (Proxy)**：添加强制代理的域名。

---

## 🔍 历史遗留问题根因分析与技术修复方案

在之前的开发与优化迭代中，应用曾遇到**特权弹窗过多**、**断开连接导致断网**、**钥匙串弹窗频繁**及**延迟测速为 1ms/不显示**等问题。以下为具体技术细节的根因和修复方式：

### 🛠 根因一：启动和连接时频繁弹出 macOS 特权授权框 (Sudo Prompt)
*   **根因**：
    1. **冗余且暴力的清理指令**：原程序在启动或断开连接时，会无条件通过 `sudo` 运行多达十几个命令行指令来重置网络状态（如清除 `0~10` 号 `utun` 路由、无视进程是否存在直接 `pkill sing-box`、禁用 HTTP/HTTPS 代理等）。若用户未配置长效免密，这些串行的 `sudo` 命令会触发 6~20 个授权弹框。
    2. **路径不匹配导致免密失效**：macOS 特权助手的免密白名单规则针对的是 `/usr/sbin/networksetup`。而在 Rust 代码中，使用的是相对路径 `"networksetup"`。这使得 macOS 安全机制无法匹配该白名单规则，从而每次都会降级要求用户输入密码。
*   **修复方案**：
    1. **引入按需清理机制**：在 Rust 侧增加前置条件判断。仅当 `singbox` 处于运行状态或 `tun.lock` 存在时才进行清理。同时，通过非特权命令读取系统代理当前状态，若代理原本就是关闭状态，则跳过 `networksetup off` 命令。
    2. **规范化特权白名单路径**：在特权助手里新增 `networksetup` 免密策略，并将 Rust 侧所有的 `"networksetup"` 调用改写为绝对路径 `"/usr/sbin/networksetup"`，实现 100% 自动免密运行。

### 🛠 根因二：频繁弹出钥匙串 (Keychain) 机密信息授权框
*   **根因**：
    - 客户端使用 `keyring` 依赖包来存取用户 Access Token 和配置签名密钥。但是，由于开发阶段使用 `tauri dev` 每次重新编译生成的二进制文件签名会发生变化（或未进行 Apple 开发者证书签名），macOS 钥匙串服务判定其为“未经授权的恶意程序企图窃取其他应用的钥匙串”，从而在每次调取、保存凭据时都弹出警示要求输入钥匙串密码。
*   **修复方案**：
    - **避开 Keychain 并保障文件安全**：不再使用 `keyring` 系统钥匙串，改为将凭据以高强度加密存放在用户的应用数据目录中（`~/.tovpn_signing_key` 以及 `App Data/.credentials.json`）。
    - **严格权限锁**：在创建和写入这些文件时，强制指定 Unix 文件权限为 `0o600`（仅当前所有者有读写权限，系统其他用户、其他程序均无法读取）。此操作彻底屏蔽了钥匙串调用的限制，同时保证了绝对的安全性。

### 🛠 根因三：连接后无法访问网页，或者异常退出程序后整机断网
*   **根因**：
    1. **路由冲突与生命周期脱节**：之前版本中，VPN 的路由规则（如添加默认路由指向 `utun`，删除原默认路由）由 Rust 代码在外部手动执行 shell 命令。如果进程意外退出、崩溃或未正常断开，这些路由残留会导致物理网卡的流量依然被导入一个已经不存在的虚拟接口，引发整机断网。
    2. **未排除服务器 IP (网络环路)**：在连接时，若未在路由表建立针对“加速服务器 IP”本身的直连路由，会导致加速程序发出的加密网络流量又被 TUN 网卡捕获，导致流量在本地无限循环。
*   **修复方案**：
    1. **原生路由接管 (Native auto_route)**：在 [tun.rs](file:///Users/dalen/dev/front/myCode/to-vpn/src-tauri/src/vpn/singbox/tun.rs) 中，开启 sing-box 内核的 `auto_route` 和 `strict_route` 参数，将分流和网卡生命周期**完全托管给内核**。当 sing-box 进程退出时，操作系统会自动原子化销毁 `utun` 网卡，路由自动恢复，彻底解决断网。
    2. **服务器 IP 静态直连**：配置生成器在生成 sing-box 配置时，会自动在最头部添加一条对“服务器 IP/32”的直连规则（outbound 为 `direct`），完美避免了网络环路。

### 🛠 根因四：服务器测速结果异常 (显示假延迟 1ms / 高延迟被掐断)
*   **根因**：
    1. **Pinia 状态不同步导致的 fake ping**：当点击服务器页面或刷新时，Pinia Store 的 `isConnected` 在页面刚挂载时还没来得及与 Rust 后端同步。此时测速代码判定 VPN 未连接，使用 ICMP 物理测速。然而实际上此时 TUN 接口已经工作，导致系统发出的 ICMP 探测包被本地的 `sing-box` (gvisor 协议栈) 拦截并就地回复，因而测试出了假延迟 `1ms` 或 `2ms`。
    2. **API 超时竞争冲突**：为了显示真实的代理连通延迟，应用在连接后会请求 sing-box Clash API 进行测速。API 默认超时为 5 秒。但是在 Rust 后端，请求该接口的 reqwest Client 超时时间被硬编码为了 3 秒。当测试较慢节点时，API 仍在测试，而 Rust 却在 3 秒整直接强行掐断并报错，导致这部分较慢但可用的节点被当做离线处理。
*   **修复方案**：
    1. **直连后端查询 VPN 状态**：测速模块在检测 VPN 是否连接时，不再使用 Pinia 本地缓存，而是通过 Tauri IPC 直接实时调取 Rust 的 `check_vpn_status` 获取真实连接状态。只要发现 VPN 在线，就强制使用 **API 代理测速模式**，避开了 ICMP 的物理拦截干扰。
    2. **测速加载态与测速超时对齐**：
       - 在 [ServerItem.vue](file:///Users/dalen/dev/front/myCode/to-vpn/src/components/servers/ServerItem.vue) 中优化了逻辑：只要服务器 API 处于 online 状态，节点就可以被正常点击。在测速期间，延迟位置显示精美蓝色呼吸点和 `- ms`。
       - 将 Rust 测速客户端的 timeout 从 3 秒提升至 **6 秒**，确保慢节点测速结果能够被完整接收，不再发生虚假的高延迟报错。

---

## 📋 常见问题与排除方法 (FAQ)

### Q1: 运行 SOCKS 模式能加速浏览器吗？
- 默认情况下，SOCKS 模式只会开放本地 `127.0.0.1:1080` 端口。如果您希望浏览器使用该加速，请在 Chrome 中安装 `SwitchyOmega` 插件，添加 SOCKS5 代理配置指向 `127.0.0.1:1080`。如果需要无感全局加速，请直接在设置里选择 **TUN 模式**。

### Q2: 切换到 TUN 模式时报错 "System Extension Required" 怎么办？
- 说明特权助手被系统拦截或未被安装。请前往 **偏好设置 -> 系统助手**，点击 **“卸载助手”** 随后再点击 **“安装助手”**。在弹出密码框中输入正确的开机密码授权即可。

### Q3: 为什么部分服务器节点测速显示为 “无法连接”？
- 证明该节点的服务器 IP 在当前网络下已失效、超时或服务器正在停机维护。系统已自动置灰并禁用了该节点，请选择其他带有实时延迟数值（如 `56ms`）的节点进行连接。
